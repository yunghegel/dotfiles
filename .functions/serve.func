# serve: Starts a simple HTTP server in the current directory.
# Usage: serve [port]
serve() {
    local port="${1:-8000}"
    echo "Serving current directory on http://localhost:$port"
    python3 -m http.server "$port"
}
